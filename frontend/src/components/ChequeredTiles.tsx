import { Box } from "@mui/material";

export const ChequeredTiles = () => {
    return (
        <>
            <Box />
            <Box sx={{ backgroundColor: 'background.paper' }} />
            <Box sx={{ backgroundColor: 'background.paper' }} />
            <Box />
        </>
    );
}