import { styled, Stack } from "@mui/material";

export const ContentStack = styled(Stack)({
    justifyContent: 'center',
    alignItems: 'center',
    position: 'absolute',
    width: '100vw',
    height: '100vh',
});